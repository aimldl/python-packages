Datasets used by the notebooks in `../ipynb/` and the scripts in `../scripts/`.

* gapminder.tsv
  * is from https://github.com/jennybc/gapminder
* gapminder-master.zip
  * is the downloaded file from the github.
* All other files
  * are the book's original datasets, downloaded from the "Pandas for Everyone" repo (see `raw-data-urls.txt`).
